def grammar_rule_processor(rule, sentence):
    # Sentence should be given as a list of dictionaries (each dictionary
    # will be a morpheme with its whole info fields. The list will be the
    # processed sentence

    # Read the rule and process to test if it match the given sentence
    ## Deconstruct rule into its components

    ## Analyze each component in order against the sentence

    ## If every rule component gives a 'true' value, then the sentence
    ## complies with the rule. Return true, else return false.
